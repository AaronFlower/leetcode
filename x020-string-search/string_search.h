#ifndef STRING_SEARCH__
#define STRING_SEARCH__

#include <string.h>
#include <stdio.h>

int naiveSearch(const char *haystack, const char *needle, size_t start);

void foo() {
    printf("Hello");
}
#endif
